void M1C::sapxep() 
{
    for (int i = 0; i < spt - 1; i++) {
        for (int j = i + 1; j < spt; j++) {
            if (a[i] > a[j]) {
                int temp = a[i];
                a[i] = a[j];
                a[j] = temp;
            }
        }
    }
    cout << "Ða sap xep mang tang dan!\n";
}
